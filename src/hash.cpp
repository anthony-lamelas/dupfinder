#include "../include/hash.hpp"


using Hash = std::string;

std::unordered_map<
    uintmax_t,
    std::vector<Hash, std::vector<std::filesystem::path>>
    > 
check_hashes(std::unordered_map<uintmax_t, std::vector<std::filesystem::path>>& size_groups){
    
}
